<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.discoverbuc">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DiscoverBuc"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity android:name=".Menu.UserMenu"></activity>
        <activity android:name=".Register2.PasswordRecovery.CreateNewPass" />
        <activity android:name=".Register2.PasswordRecovery.PasswordChanged" />
        <activity android:name=".Register2.PasswordRecovery.ForgetPass" />
        <activity android:name=".Register2.Register.Verification" />
        <activity android:name=".Dashboard" />
        <activity android:name=".Register2.Register.RegisterPreferences" />
        <activity android:name=".Register2.Register.RegisterName" />
        <activity android:name=".Register2.Register.Register" />
        <activity android:name=".Register2.Login.Login" />
        <activity android:name=".Register2.StartupScreen" />
        <activity
            android:name=".Register1.WelcomeActivity"
            android:label="@string/title_activity_welcome"
            android:theme="@style/Theme.DiscoverBuc.NoActionBar" />
        <activity android:name=".Common.LoadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>