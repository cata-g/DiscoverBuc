<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Register2.Register.Verification"
    android:transitionName="transition_verification_page"
    android:background="@color/pale_yellow"
    >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_margin="10dp">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="10dp">

            <ImageButton
                android:id="@+id/close_validation"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:src="@drawable/icon_close"
                android:scaleType="centerCrop"
                android:background="#00000000"
                android:onClick="closeVerif"
                android:transitionName="transition_next_button"
                />

            <ImageView
                android:id="@+id/image_starter"
                android:layout_width="match_parent"
                android:layout_height="300dp"
                android:src="@drawable/validation"
                />

        </RelativeLayout>


        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/verify_code"
            android:textSize="40sp"
            android:textAllCaps="true"
            android:layout_marginTop="20dp"

            android:layout_gravity="center"
            android:fontFamily="@font/squada_one"
            android:textColor="@color/light_black"
            />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/verify_code_text"
            android:textSize="30sp"
            android:fontFamily="@font/squada_one"
            android:textColor="@color/light_black"
            android:layout_gravity="center"
            android:layout_marginTop="50dp"
            />

        <com.goodiebag.pinview.Pinview
            android:id="@+id/pinView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:pinWidth="50dp"
            app:pinHeight="50dp"
            app:pinLength="6"
            app:inputType="number"
            app:password="false"
            app:splitWidth="10dp"
            android:layout_marginTop="20dp"
            android:layout_marginBottom="20dp"/>

        <Button
            android:id="@+id/next_verifyButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/next_button"
            android:layout_marginTop="50dp"
            android:onClick="Finish"
            android:transitionName="transition_login"
            android:textColor="@color/light_black"
            android:fontFamily="@font/squada_one"
            android:textSize="20sp"
            android:background="@drawable/button_background"
            />


    </LinearLayout>

</ScrollView>